# 🎮 Игра "Угадай число"

## Описание проекта
Консольная игра на Python, где игрок должен угадать случайное число от 1 до 100. Программа дает подсказки "больше" или "меньше" и считает количество попыток.

## Задания для команды

### Для GitHub-менеджера:
1. Создать репозиторий `guess-number-game-team-X`
2. Склонировать репозиторий локально
3. Создать структуру проекта

### Для Разработчика:
1. Создать ветку `feature/game-logic`
2. Реализовать основную логику игры
3. Добавить обработку ошибок ввода

### Для Тестировщика:
1. Создать ветку `feature/testing`
2. Протестировать все сценарии игры
3. Создать файл с тест-кейсами

## Базовый код для начала

```python
import random

def guess_number_game():
    """Основная функция игры 'Угадай число'"""
    print("🎮 Добро пожаловать в игру 'Угадай число!'")
    print("Я загадал число от 1 до 100. Попробуйте его угадать!")
    
    # Генерируем случайное число
    secret_number = random.randint(1, 100)
    attempts = 0
    max_attempts = 7  # Ограничение попыток для усложнения
    
    while attempts < max_attempts:
        try:
            # Получаем ввод от пользователя
            guess = int(input(f"\nПопытка {attempts + 1}/{max_attempts}: "))
            attempts += 1
            
            # Проверяем угадано ли число
            if guess == secret_number:
                print(f"🎉 Поздравляю! Вы угадали число {secret_number} за {attempts} попыток!")
                return
            elif guess < secret_number:
                print("📈 Загаданное число больше!")
            else:
                print("📉 Загаданное число меньше!")
                
        except ValueError:
            print("❌ Пожалуйста, введите корректное число!")
            continue
    
    # Если попытки закончились
    print(f"😔 Игра окончена! Загаданное число было: {secret_number}")

def main():
    """Главная функция программы"""
    while True:
        guess_number_game()
        
        # Предложить сыграть еще раз
        play_again = input("\nХотите сыграть еще раз? (да/нет): ").lower()
        if play_again not in ['да', 'yes', 'y', 'д']:
            print("Спасибо за игру! До свидания! 👋")
            break

if __name__ == "__main__":
    main()
```

## Дополнительные возможности для расширения

### Уровень 1 (базовый):
- Добавить подсчет лучшего результата
- Сохранять историю игр в список

### Уровень 2 (средний):
- Добавить уровни сложности (разные диапазоны чисел)
- Система подсказок (четное/нечетное)
- Сохранение статистики в файл

### Уровень 3 (продвинутый):
- Графический интерфейс на tkinter
- Многопользовательский режим
- Веб-версия на Flask

## Структура проекта
```
guess-number-game/
├── main.py           # Основной файл с игрой
├── game.py          # Логика игры (отдельный модуль)
├── stats.py         # Статистика игр
├── README.md        # Документация
├── tests.md         # Описание тест-кейсов
└── .gitignore       # Игнорируемые файлы
```

## Git workflow для команды

### 1. Начальная настройка:
```bash
# Клонирование репозитория
git clone [URL репозитория]
cd guess-number-game-team-X

# Настройка Git
git config user.name "Ваше имя"
git config user.email "ваш@email.com"
```

### 2. Разработка в ветках:
```bash
# Создание feature-ветки
git checkout -b feature/game-logic

# Добавление изменений
git add .
git commit -m "Feat: add basic game logic"

# Отправка на GitHub
git push origin feature/game-logic
```

### 3. Code Review и слияние:
```bash
# Создание Pull Request на GitHub
# После одобрения - слияние в main
git checkout main
git pull origin main
```

## Критерии оценки

### ✅ Обязательные элементы:
- [ ] Игра работает корректно
- [ ] Есть обработка некорректного ввода  
- [ ] Минимум 3 коммита с осмысленными сообщениями
- [ ] README заполнен по шаблону
- [ ] Код оформлен и прокомментирован

### 🌟 Дополнительные баллы:
- [ ] Добавлены уровни сложности
- [ ] Реализована система рекордов
- [ ] Красивый текстовый интерфейс
- [ ] Файл с тестами создан
- [ ] Код разделен на модули

## Полезные ссылки
- [Python Random Module](https://docs.python.org/3/library/random.html)
- [Python Input/Output](https://docs.python.org/3/tutorial/inputoutput.html)
- [Git Commands Cheatsheet](https://git-scm.com/docs)

---
**Время на разработку:** 1.5-2 часа  
**Сложность:** Начинающий  
**Язык:** Python 3.6+