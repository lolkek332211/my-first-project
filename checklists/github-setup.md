# 🔧 Инструкция по настройке GitHub - УП.05

## 🎯 Цель
Пошаговая инструкция для студентов по созданию аккаунта GitHub, настройке Git и созданию первого репозитория.

---

## ШАГ 1: Создание аккаунта на GitHub (15 мин)

### 1.1 Открыть сайт GitHub
1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите на сайт: **[github.com](https://github.com)**
3. Нажмите кнопку **"Sign up"** (Регистрация)

### 1.2 Заполнение формы регистрации
**Заполните данные:**

**Username (имя пользователя):**
- ❌ НЕ используйте: `user123`, `student2025`, `qwerty`
- ✅ ИСПОЛЬЗУЙТЕ: `ivan-petrov-dev`, `maria-frontend`, `alex-python`
- 💡 Это будет ваше профессиональное имя!

**Email address:**
- Используйте рабочую почту (Gmail, Mail.ru, Yandex)
- Обязательно запомните - нужна для подтверждения

**Password:**
- Минимум 8 символов
- Включите цифры и буквы
- Запишите в блокнот!

### 1.3 Подтверждение аккаунта
1. После регистрации проверьте почту
2. Найдите письмо от GitHub (проверьте папку "Спам")
3. Нажмите ссылку подтверждения
4. Выберите **Free plan** (бесплатный план)

### 1.4 Настройка профиля (опционально)
- Добавьте фото профиля
- Заполните краткое описание
- Укажите местоположение

---

## ШАГ 2: Установка и настройка Git (10 мин)

### 2.1 Проверка установки Git
Откройте терминал/командную строку:
- **Windows:** `Win + R` → `cmd` → Enter
- **macOS:** `Cmd + Space` → "Terminal"
- **Linux:** `Ctrl + Alt + T`

Выполните команду:
```bash
git --version
```

**Результат:**
- ✅ Если показывает версию (например, `git version 2.39.0`) → Git установлен
- ❌ Если ошибка "команда не найдена" → нужно установить

### 2.2 Установка Git (если нужно)
**Windows:**
1. Скачайте с [git-scm.com](https://git-scm.com)
2. Установите с настройками по умолчанию
3. Перезапустите терминал

**macOS:**
```bash
# Установка через Homebrew
brew install git

# Или скачать с git-scm.com
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install git
```

### 2.3 Настройка пользователя Git
Выполните команды, заменив данные на свои:

```bash
# Установка имени (как на GitHub)
git config --global user.name "Ваше Имя"

# Установка email (как при регистрации на GitHub)
git config --global user.email "ваш@email.com"
```

**Пример:**
```bash
git config --global user.name "Ivan Petrov"
git config --global user.email "ivan.petrov@gmail.com"
```

### 2.4 Проверка настройки
```bash
# Проверить имя
git config user.name

# Проверить email  
git config user.email
```

---

## ШАГ 3: Создание первого репозитория (10 мин)

### 3.1 Создание репозитория на GitHub
1. Войдите в свой аккаунт GitHub
2. Нажмите **"+"** в правом верхнем углу
3. Выберите **"New repository"**

### 3.2 Настройка репозитория
**Repository name:** `first-project-team-[номер]`
- Пример: `first-project-team-1`

**Description:** `Первый проект команды [название] по УП.05`
- Пример: `Первый проект команды "Кодеры" по УП.05`

**Настройки:**
- ✅ **Public** (публичный)
- ✅ **Add a README file** (добавить README)
- ❌ .gitignore (пока не нужно)
- ❌ License (пока не нужно)

3. Нажмите **"Create repository"**

### 3.3 Копирование ссылки репозитория
1. На странице созданного репозитория найдите зеленую кнопку **"Code"**
2. Выберите вкладку **"HTTPS"**
3. Скопируйте ссылку (например: `https://github.com/username/first-project-team-1.git`)

---

## ШАГ 4: Клонирование репозитория локально (10 мин)

### 4.1 Создание папки для проектов
```bash
# Windows
cd C:\
mkdir Projects
cd Projects

# macOS/Linux  
cd ~
mkdir Projects
cd Projects
```

### 4.2 Клонирование репозитория
```bash
# Замените URL на ваш репозиторий
git clone https://github.com/ваш-username/first-project-team-X.git

# Переход в папку проекта
cd first-project-team-X
```

### 4.3 Проверка содержимого
```bash
# Показать файлы в папке
ls       # macOS/Linux
dir      # Windows

# Должен показать файл README.md
```

---

## ШАГ 5: Первый коммит и push (10 мин)

### 5.1 Создание файла
Создайте файл `hello.py` с содержимым:
```python
print('Hello, GitHub!')
print('Команда: [ваше название команды]')
print('УП.05 - Учебная практика')
```

**Способы создания:**
- Блокнот/текстовый редактор
- VS Code
- Командная строка: `echo "print('Hello, GitHub!')" > hello.py`

### 5.2 Добавление файла в Git
```bash
# Показать статус (какие файлы изменены)
git status

# Добавить файл в индекс
git add hello.py

# Или добавить все файлы
git add .
```

### 5.3 Создание коммита
```bash
# Создать коммит с сообщением
git commit -m "Initial commit: add hello.py"
```

### 5.4 Отправка на GitHub
```bash
# Отправить изменения на GitHub
git push origin main
```

### 5.5 Проверка результата
1. Обновите страницу репозитория на GitHub
2. Убедитесь, что файл `hello.py` появился
3. Проверьте, что коммит отображается в истории

---

## 🎉 Результат
Поздравляем! Теперь у вас есть:
- ✅ Аккаунт на GitHub
- ✅ Настроенный Git на компьютере  
- ✅ Первый репозиторий
- ✅ Первый коммит и push

---

## 🚨 Решение частых проблем

### Проблема: Git не распознается как команда
**Решение:**
1. Установите Git с [git-scm.com](https://git-scm.com)
2. Перезапустите терминал
3. Проверьте: `git --version`

### Проблема: Ошибка авторизации при push
**Решение:**
1. Проверьте настройки: `git config user.email`
2. Используйте Personal Access Token вместо пароля
3. Обратитесь к преподавателю

### Проблема: Файл не отображается на GitHub
**Решение:**
1. Проверьте статус: `git status`
2. Убедитесь, что сделали `git add` и `git commit`
3. Выполните `git push origin main`

### Проблема: Ошибка "repository not found"
**Решение:**
1. Проверьте правильность URL репозитория
2. Убедитесь, что репозиторий публичный
3. Проверьте права доступа

---

## 📞 Получение помощи

### В классе:
1. Обратитесь к преподавателю
2. Попросите помощь у одноклассников
3. Поработайте в паре/команде

### Онлайн-ресурсы:
- [GitHub Docs](https://docs.github.com)
- [Git Book (русский)](https://git-scm.com/book/ru/v2)
- [GitHub Desktop](https://desktop.github.com) - графический интерфейс

### Полезные команды для справки:
```bash
# Показать статус репозитория
git status

# Показать историю коммитов
git log --oneline

# Показать удаленные репозитории
git remote -v

# Помощь по команде
git help <команда>
```

---

**🎯 Главное:** Не бойтесь экспериментировать! Git сохраняет всю историю, поэтому вы всегда можете вернуться к предыдущей версии.

*Удачи в освоении Git и GitHub! 🚀*